---
icon: file
---

# 2. XSS en el DOM por PP en Cliente

## 🧪 XSS en el DOM por prototype pollution en cliente

### 📄 Descripción del laboratorio

Este laboratorio es vulnerable a XSS DOM a través de la contaminación de prototipos del lado del cliente. Para resolver el laboratorio:

Encuentra una fuente que puedas usar para añadir propiedades arbitrarias al Object.prototype global.

Identifica una propiedad gadget que te permita ejecutar JavaScript arbitrario.

Combina estos para llamar a alert().

Puedes resolver este laboratorio manualmente en tu navegador, o usar DOM Invader para ayudarte.

***

### 📚 Teoria

En esta clase exploramos una vulnerabilidad de client-side prototype pollution que nos permite ejecutar JavaScript arbitrario manipulando **Object.prototype** desde la URL.

Identificamos una fuente de contaminación mediante el parámetro ‘**\_\_proto\_\_**‘ y analizamos los archivos JavaScript cargados por la página. En el archivo ‘**searchLogger.js**‘, descubrimos un gadget donde, si existe la propiedad ‘**transport\_url**‘ en el objeto **config**, se inserta dinámicamente una etiqueta script en el DOM con un **src** controlable.

Aprovechamos esto para inyectar un valor malicioso que ejecuta ‘**alert(1)**‘, demostrando cómo una simple manipulación del prototipo puede derivar en una vulnerabilidad de XSS crítica.

***

### 📝 Practica

Hemos confirmado que es posible **contaminar el prototipo** (Object.prototype).

<figure><img src="../../../.gitbook/assets/image (1171).png" alt=""><figcaption></figcaption></figure>

Volvemos al depurador y localizamos de nuevo un fragmento de script interesante. Procedemos a analizarlo en detalle.

<figure><img src="../../../.gitbook/assets/image (1172).png" alt=""><figcaption></figcaption></figure>

Observamos que existe un objeto config que, en su definición inicial, únicamente contiene una propiedad: params.

Lo que podemos hacer como atacantes es **contaminar el prototipo** de manera que la propiedad transport\_url herede el valor alert(1) (o cualquier cadena que queramos ejecutar).

De esta forma, cuando el código de la aplicación asigne o lea el atributo src de un elemento (por ejemplo, un \<img>, \<iframe>, \<script>, etc.) a partir de config.transport\_url, este tomará el valor inyectado:

Al heredar alert(1) desde el prototipo, el atributo src acabará conteniendo javascript:alert(1) (o directamente alert(1) según el contexto), lo que desencadenará la ejecución del código JavaScript arbitrario → **XSS** exitoso.

<figure><img src="../../../.gitbook/assets/image (1173).png" alt=""><figcaption></figcaption></figure>

Laboratorio resuelto:

<figure><img src="../../../.gitbook/assets/image (1174).png" alt=""><figcaption></figcaption></figure>
