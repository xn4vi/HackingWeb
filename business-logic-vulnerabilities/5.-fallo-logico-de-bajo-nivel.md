---
icon: file
---

# 5. Fallo Lógico de Bajo Nivel

## 🧑‍💻 Fallo lógico de bajo nivel (integer overflow)

### 📄 Descripción del laboratorio

Este laboratorio presenta una vulnerabilidad de **integer overflow** en la lógica de cálculo del carrito.

Al añadir una cantidad muy elevada de productos, el total supera el valor máximo permitido por el tipo de dato entero del backend, provocando un desbordamiento.

El objetivo es:

* Manipular el valor total del carrito mediante overflow
* Ajustarlo a un valor dentro del crédito disponible
* Completar la compra del producto

***

### 📚 Teoría

Algunos lenguajes de programación utilizan tipos de datos enteros con un límite máximo.

Un entero típico de 32 bits tiene como valor máximo:

```
2,147,483,647
```

#### 📌El fallo

Cuando el valor supera ese límite:

* Se produce un **overflow**
* El número pasa al rango negativo

Ejemplo conceptual:

```
MAX_INT + 1 → valor negativo
```

Esto provoca que:

* El precio total del carrito se vuelva negativo
* El sistema continúe operando con ese valor incorrecto

#### 📌 Impacto

Esto permite:

* Manipular el total del carrito
* Reducir precios de forma artificial
* Forzar valores dentro del crédito disponible

***

### 📝 Práctica

#### 1️⃣ Interceptar la petición

Nos logueamos en la aplicación.

Interceptamos la petición de añadir productos al carrito.

Observamos un parámetro de cantidad:

```
quantity=1
```

***

#### 2️⃣ Identificar el límite

Probamos a añadir unidades manualmente.

Observamos que el sistema permite como máximo:

```
99
```

unidades por petición.

***

#### 3️⃣ Automatizar el envío

Enviamos la petición a Intruder.

<figure><img src="../../../.gitbook/assets/image (1376).png" alt=""><figcaption></figcaption></figure>

Configuramos el ataque para repetir la petición múltiples veces.

<figure><img src="../../../.gitbook/assets/image (1376).png" alt=""><figcaption></figcaption></figure>

Creamos un **resource pool** para controlar la velocidad de envío.

<figure><img src="../../../.gitbook/assets/image (1377).png" alt=""><figcaption></figcaption></figure>

El objetivo es:

* Incrementar progresivamente el número de unidades
* Observar cómo evoluciona el total del carrito

***

#### 4️⃣ Detectar el overflow

Ejecutamos el ataque.

Mientras tanto, observamos el carrito en el navegador.

<figure><img src="../../../.gitbook/assets/image (1378).png" alt=""><figcaption></figcaption></figure>

Llega un punto en el que:

* El precio total se vuelve negativo

Esto ocurre porque:

* Se ha superado el límite del entero
* El valor ha desbordado al rango negativo

<figure><img src="../../../.gitbook/assets/image (1379).png" alt=""><figcaption></figcaption></figure>

***

#### 5️⃣ Ajustar el valor final

A partir de ese punto:

* Seguimos añadiendo productos
* El valor continúa incrementándose desde el rango negativo

El comportamiento es cíclico:

* Positivo → overflow → negativo → vuelve a subir

Ajustamos el número de peticiones hasta:

* Alcanzar un valor cercano a 0
* O ligeramente positivo y dentro de nuestro crédito

<figure><img src="../../../.gitbook/assets/image (1380).png" alt=""><figcaption></figcaption></figure>

***

#### 6️⃣ Completar la compra

Cuando el precio es suficientemente bajo:

* Procedemos al checkout
* Completamos la compra
